/target
/.cargo/
node_modules/

# Per-install spotlight usage stats (src/spotlight.rs's Frecency), written
# in whatever --config-dir the daemon was started with. Runtime state, not
# configuration -- never check this in.
/config/spotlight_frecency.json

# BIG TODO: Think about it if it really makes sense to have it at all because at the end I have a input keymap and the applications later try to do it if they have it implemented and else they just report that.